program{
	for i min {7, 5, 8, 3, 9, 6, 4, 2, 1, 0} do {
        #	using
         #  	 	bool i;         # esconde la variable `i` del for
       	#	in
        	print i;       # siempre imprime `false`
	};
}

