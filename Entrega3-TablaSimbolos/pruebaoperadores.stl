program {
  using
    int  a, b; # inicializan en 0
    set  c, d; # inicializan en false
    bool e, f; # inicializan en {}
  in

  a = 10;
  scan b;
  c = {1,2,3};
  d = {2,3,4};
  e = true;
  scan f;

  print "first: ", b, ", ";
  println "second: ", f;

  if (b @ c) 
     if (b @ c)
     println "improbable"
  else {
    println "probable";
  };

}